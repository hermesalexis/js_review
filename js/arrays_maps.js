//Arrays
//const car = ['Product1', 'Product2', 'Product3'];

// const appContainer = document.querySelector('#app');

// let html = '';
// car.forEach(product => {
//     html += `<li>${product}</li>`;
// })

// appContainer.innerHTML = html;

// car = ['Product1', 'Product2', 'Product3'];

// car.map(product => {
//     return `The product is ${product}`;
// });

//Objects keys

const person = {
    name: 'Hermes',
    profession: 'Web developer',
    age: 30
}

// const {name, profession} = person;
// console.log(`Name: ${name} profession: ${profession}`);

console.log(Object.keys(person));